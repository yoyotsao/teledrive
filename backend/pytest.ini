[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers --tb=short
markers =
    real: touches a real external service (Telegram / the live site). Never runs in the default suite.
filterwarnings =
    ignore::DeprecationWarning
